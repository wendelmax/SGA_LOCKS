<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <nav>
            <ul>
                <% if (typeof userName !== "undefined") { %>
                    <li>Bem-vindo, <%= userName %></li>
                    <li><a href="/logout">Logout</a></li>
                <% } else { %>
                    <li><a href="/loginAdm">Login</a></li>
                    <li><a href="/registerAdm">Registrar</a></li>
                <% } %>
            </ul>
        </nav>
    </header>
    <main>
        <%- body %>
    </main>
</body>
</html>
