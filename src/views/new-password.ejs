<!DOCTYPE html>
<html lang="pt-br">
<head>
  <link rel="icon" href="/logo.svg" type="image/x-icon">
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Recuperação de Senha</title>
  <style>
    /* Estilos Globais */
    body {
        font-family: 'Arial', sans-serif;
        background: url('/fundo.svg') repeat;
        margin: 0;
        padding: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
    }

    /* Container do formulário */
    .container {
        background-color: #fff;
        padding: 40px;
        border-radius: 12px;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        text-align: center;
        width: 100%;
        max-width: 450px;
    }

    /* Título */
    h1 {
        font-size: 28px;
        color: #333;
        margin-bottom: 20px;
    }

    /* Texto de erro */
    .error-message {
        color: #ff4d4d;
        font-size: 14px;
        margin-bottom: 20px;
        font-weight: bold;
    }

    /* Labels */
    label {
        display: block;
        font-size: 14px;
        color: #555;
        margin-bottom: 10px;
        text-align: left;
    }

    /* Input de e-mail */
    input[type="email"] {
        width: 100%;
        padding: 12px;
        margin-bottom: 20px;
        border: 1px solid #ccc;
        border-radius: 8px;
        font-size: 14px;
        box-sizing: border-box;
    }

    /* Botão */
    button {
        background-color: #2c6b96;
        color: white;
        border: none;
        border-radius: 8px;
        padding: 12px 0;
        font-size: 16px;
        width: 100%;
        cursor: pointer;
        transition: background-color 0.3s ease;
    }

    button:hover {
        background-color:#1a4d6d;
        ;
    }

    /* Link de redirecionamento */
    p a {
        color: #5b9ed6;
        font-weight: bold;
        text-decoration: none;
        font-size: 14px;
    }

    p a:hover {
        text-decoration: underline;
    }
    img{
      width: 70%;
      height: 90px;
    }
  </style>
</head>
<body>
  <div class="container">
    <img src="logo.svg" alt="">
    <h1>Recuperação de Senha</h1>

    <% if (error) { %>
      <p class="error-message"><%= error %></p>
    <% } %>

    <form action="/reset-password" method="POST">
      <label for="email">Digite sua nova senha:</label>
      <input type="email" id="email" name="email" placeholder="Digite sua nova senha" required>
      <button type="submit">redfinir senha</button>
    </form>

    <p>Já tem uma conta? <a href="/login">Faça login aqui.</a></p>
  </div>
</body>
</html>
